---
import "../styles/global.css";
import type { Locale } from "../features/i18n/types";

interface Props {
  title?: string;
  description?: string;
  locale?: Locale;
}

const {
  locale = "es",
  title = "Qably",
  description = locale === "es"
    ? "Plataforma de QA automatizada nativa con IA, telemetría de CI/CD en tiempo real y trazabilidad bidireccional desde el commit hasta el caso de prueba."
    : "AI-native automated QA management, real-time CI/CD telemetry, and bi-directional traceability from git commit to test execution.",
} = Astro.props;

const isEn = locale === "en";
const canonicalUrl = new URL(Astro.url.pathname, Astro.site || "https://qably.app");
const siteUrl = Astro.site ? Astro.site.toString() : "https://qably.app";
---

<!doctype html>
<html lang={locale} class="dark scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/svg+xml" href="/qably-icon.svg" />
    <meta name="generator" content={Astro.generator} />

    <!-- Canonical and Alternate Languages (i18n SEO 2026) -->
    <link rel="canonical" href={canonicalUrl} />
    <link rel="alternate" hreflang="es" href={`${siteUrl}/`} />
    <link rel="alternate" hreflang="en" href={`${siteUrl}/en`} />
    <link rel="alternate" hreflang="x-default" href={`${siteUrl}/`} />

    <!-- SEO Meta Tags -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="robots" content="index, follow" />
    <meta name="theme-color" content="#000000" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="/qably-sidebar.png" />
    <meta property="og:locale" content={isEn ? "en_US" : "es_ES"} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalUrl} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content="/qably-sidebar.png" />

    <!-- Google Fonts: Geist Sans & Geist Mono (Vercel Style) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Geist:wght@300;400;500;600;700&family=Geist+Mono:wght@400;500;600&display=swap"
      rel="stylesheet"
    />

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json" is:inline define:vars={{ title, description, siteUrl }}>
      {
        "@context": "https://schema.org",
        "@graph": [
          {
            "@type": "Organization",
            "name": "Qably",
            "url": siteUrl,
            "logo": siteUrl + "/qably-icon.svg",
            "description": "AI-automated software quality management for modern engineering teams."
          },
          {
            "@type": "SoftwareApplication",
            "name": "Qably",
            "applicationCategory": "DeveloperApplication",
            "operatingSystem": "All",
            "offers": {
              "@type": "Offer",
              "price": "0",
              "priceCurrency": "USD"
            },
            "description": description
          }
        ]
      }
    </script>
  </head>
  <body class="bg-black text-white min-h-screen selection:bg-white selection:text-black font-sans antialiased overflow-x-hidden">
    <!-- Accessibility Skip Link -->
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-white focus:text-black focus:rounded-md focus:font-semibold focus:shadow-xl"
    >
      {isEn ? "Skip to main content" : "Saltar al contenido principal"}
    </a>

    <!-- Screen reader & search engine hidden headline -->
    <h1 class="sr-only">
      {title === "Qably"
        ? (isEn ? "Qably — QA Management for Engineers, Automated by AI" : "Qably — Gestión QA para Ingenieros, Automatizada con IA")
        : title}
    </h1>

    <div class="relative min-h-screen flex flex-col justify-between">
      <slot />
    </div>
  </body>
</html>
